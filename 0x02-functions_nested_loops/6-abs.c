#include "main.h"

/**
 * _abs - Absolute value of an integer
 *
 * @n: Integer parameter
 *
 * Return: 0 if successful
 *
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
