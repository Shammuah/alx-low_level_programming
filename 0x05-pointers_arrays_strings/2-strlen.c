#include "main.h"
#include <stdio.h>

/**
 * _strlen - Returns lenght of a string
 * @s: String parameter
 * Return: Always 0
 */

int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != 0; c++)
	{
	}
	return (c);
}
